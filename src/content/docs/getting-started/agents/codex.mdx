---
title: Codex
description: Use Codex in Neurodesk, the coding agent from OpenAI
sidebar:
  order: 2
---

Codex is OpenAI's command-line coding agent. You can run it inside a Neurodesk JupyterHub terminal and authenticate with your OpenAI account.

**1. Start Codex in a terminal.**

```bash
codex
```

**2. Sign in using the Device Code option.**

<div class="fake not-content">
	<div class="fakeMenu">
		<div class="fakeButtons fakeClose"></div>
		<div class="fakeButtons fakeMinimize"></div>
		<div class="fakeButtons fakeZoom"></div>
	</div>
	<div class="fakeScreen fakeScreen--scroll">
		<pre class="fake-log fake-log--cli">{`  Welcome to `}<strong>{`Codex`}</strong>{`, OpenAI's command-line coding agent

  Sign in with ChatGPT to use Codex as part of your paid plan
  or connect an API key for usage-based billing

  1. Sign in with ChatGPT
     `}<span class="fake-cli-dim">{`Usage included with Plus, Pro, Team, and Enterprise plans`}</span>{`

`}<span class="fake-cli-sel">{`> 2. Sign in with Device Code
     Sign in from another device with a one-time code`}</span>{`

  3. Provide your own API key
     `}<span class="fake-cli-dim">{`Pay for what you use`}</span>{`

  `}<span class="fake-cli-dim">{`Press Enter to continue`}</span></pre>
	</div>
</div>

**3. Give it a task.**
